<h2>Navigation</h2>
<ul>
  <% all_chapters.each do |other_chapter| %>
    <li>
      <%=
        link_to(
          "Chapter #{other_chapter.number} - #{other_chapter.title}",
          book_chapter_path(book.slug, other_chapter.number)
        )
      %>
    </li>
  <% end %>
</ul>

<%= chapter.content_html.html_safe %>

<hr/>

<p>
  This is the end of the free preview.
  Please <%= link_to "buy the book", book_path(book.slug) %>.
</p>
